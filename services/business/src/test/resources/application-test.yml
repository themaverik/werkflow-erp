spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS hr_service\;CREATE SCHEMA IF NOT EXISTS finance_service\;CREATE SCHEMA IF NOT EXISTS procurement_service\;CREATE SCHEMA IF NOT EXISTS inventory_service\;CREATE SCHEMA IF NOT EXISTS identity_service
    driverClassName: org.h2.Driver
    username: sa
    password:
    initialization-mode: always
    continue-on-error: true
  sql:
    init:
      data-locations: classpath:h2-sequences.sql
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.hbm2ddl.create_namespaces: true
  flyway:
    enabled: false
    placeholderReplacement: false
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=10m
